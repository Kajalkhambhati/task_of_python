Write a function called string_range that takes a single number and returns a string of its range. The string characters should be separated by dots(.) For example, 
if you pass 6 as an argument, your function should return ‘0.1.2.3.4.5’.


def string_range(numbers):#define function
    list1=[]#create empty list
    for i in range(numbers):#itrate range
        list1.append(str(i))#add in empty list
    return(".".join(list1))#return output
print(string_range(4))        
